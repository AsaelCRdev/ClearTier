# Copia este archivo a ".env" (que ya está en .gitignore) y coloca tus valores reales.
# Vite solo expone al cliente las variables que empiezan con VITE_.

# URL base del backend real (Spring Boot). No se usa todavía mientras el
# proyecto corre sobre el mock backend (src/ts/api/mockBackend.ts).
VITE_API_BASE_URL=http://localhost:8081/api
